# Leave empty to use the Vite dev proxy declared in vite.config.js.
# Set it only if your API allows CORS from the frontend URL.
VITE_API_BASE_URL=

# Expected backend during local development:
# http://localhost:3001

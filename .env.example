# Root-level env is optional.
# API loads .env from this root first, then API/.env overrides it.

NODE_ENV=development
PORT=3001
APP_BASE_URL=http://localhost:3001
CLIENT_BASE_URL=http://localhost:5173
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=theway
